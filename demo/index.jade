doctype html
html
    head
        meta(charset="utf-8")
        title Hello world
    body
        h1 Refresh the document

        script(src="../bower_components/seajs/dist/sea.js")

        // startup
        script.
            seajs.config({
                base: '../'
            })
            seajs.use(['./index'], function (index) {
                index.init()
            })